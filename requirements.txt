# Core dependencies - keeping minimal as per "no deps" philosophy
flask>=2.3.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
pyyaml>=6.0
aiohttp>=3.8.0
python-dotenv>=1.0.0

# Numerical computation (required for HDDR, gravitational attention, tokenformer)
numpy>=1.24.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
